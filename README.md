# Chimera

Project Chimera — repository for research and tooling.

## Environment

- Recommended: use the project's virtual environment in `.venv`.
- pip version used in this project: 26.0.1

To replicate or upgrade pip inside the venv run:

```powershell
.venv\Scripts\python -m pip install --upgrade pip==26.0.1
.venv\Scripts\python -m pip --version
```

